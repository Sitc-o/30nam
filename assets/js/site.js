
(()=>{'use strict';const $=(s,r=document)=>r.querySelector(s),$$=(s,r=document)=>[...r.querySelectorAll(s)];const toggle=$('.menu-toggle'),nav=$('.main-nav');toggle?.addEventListener('click',()=>{nav.classList.toggle('open');document.body.classList.toggle('menu-open')});const simpleToggle=$('.simple-menu'),simpleNav=$('.simple-nav');simpleToggle?.addEventListener('click',()=>simpleNav?.classList.toggle('open'));$$('.nav-drop>button').forEach(b=>b.addEventListener('click',e=>{if(innerWidth<=1050||document.body.classList.contains('home-v11')){e.preventDefault();b.parentElement.classList.toggle('open')}}));const progress=$('.reading-progress span');const back=$('.backtop');const eventLists=$$('.event-list');const onScroll=()=>{const h=document.documentElement;const max=h.scrollHeight-innerHeight;progress.style.width=(max?scrollY/max*100:0)+'%';back.classList.toggle('show',scrollY>500);eventLists.forEach(el=>{const rect=el.getBoundingClientRect();const p=rect.height?(innerHeight*.5-rect.top)/rect.height:0;el.style.setProperty('--line-progress',Math.max(0,Math.min(1,p)))})};addEventListener('scroll',onScroll,{passive:true});onScroll();back?.addEventListener('click',()=>scrollTo({top:0,behavior:'smooth'}));const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');io.unobserve(e.target)}}),{threshold:0});$$('.reveal').forEach(x=>io.observe(x));const searchDialog=$('.search-dialog'),open=$('.search-open'),close=$('.search-close'),input=$('#globalSearch'),results=$('#searchResults');open?.addEventListener('click',()=>{searchDialog.showModal();setTimeout(()=>input.focus(),80)});close?.addEventListener('click',()=>searchDialog.close());const norm=s=>(s||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase();input?.addEventListener('input',()=>{const q=norm(input.value.trim());if(q.length<2){results.innerHTML='<p>Nhập ít nhất 2 ký tự để tìm kiếm.</p>';return}const found=(window.SITE_SEARCH_INDEX||[]).filter(x=>norm(x.title+' '+x.page+' '+x.excerpt).includes(q)).slice(0,30);results.innerHTML=found.length?found.map(x=>`<a class="search-result" href="${x.url}"><small>${x.page}</small><b>${x.title}</b><p>${x.excerpt||''}</p></a>`).join(''):'<p>Không tìm thấy nội dung phù hợp.</p>'});$$('.local-search').forEach(inp=>inp.addEventListener('input',()=>{const q=norm(inp.value);$$('.searchable-section').forEach(s=>s.classList.toggle('search-hidden',q&&!norm(s.textContent).includes(q)))}));const headings=$$('.book-chapter[id]');if(headings.length){const spy=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){$$('.toc-card a').forEach(a=>a.classList.toggle('current',a.getAttribute('href')==='#'+e.target.id))}}),{rootMargin:'-25% 0px -65% 0px'});headings.forEach(h=>spy.observe(h))}const eSearch=$('#eventSearch'),eEra=$('#eventEra');const filterEvents=()=>{const q=norm(eSearch?.value||''),era=eEra?.value||'all';$$('.event-list .event').forEach(el=>{const y=Number(el.dataset.voiceYear||0);const eraOk=era==='all'||(era==='1997-2005'&&y<=2005)||(era==='2006-2014'&&y>=2006&&y<=2014)||(era==='2015-2020'&&y>=2015&&y<=2020)||(era==='2021-2027'&&y>=2021);el.classList.toggle('search-hidden',!(eraOk&&(!q||norm(el.textContent).includes(q))))})};eSearch?.addEventListener('input',filterEvents);eEra?.addEventListener('change',filterEvents);const runCounter=el=>{if(el.dataset.done)return;const end=Number(el.dataset.target||0);const prefix=el.dataset.prefix||'';const suffix=el.dataset.suffix||'';let startTime=null;const dur=1400;const fmt=v=>Math.round(v).toLocaleString('vi-VN');const step=t=>{if(!startTime)startTime=t;const p=Math.min((t-startTime)/dur,1);const val=end*(1-Math.pow(1-p,3));el.textContent=prefix+fmt(val)+suffix;if(p<1)requestAnimationFrame(step);else{el.dataset.done='1';el.textContent=prefix+fmt(end)+suffix;el.classList.add('pop');el.addEventListener('animationend',()=>el.classList.remove('pop'),{once:true});}};requestAnimationFrame(step);};const counterObserver=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){runCounter(e.target);counterObserver.unobserve(e.target);}}),{threshold:.4});$$('.counter').forEach(el=>counterObserver.observe(el));})();
